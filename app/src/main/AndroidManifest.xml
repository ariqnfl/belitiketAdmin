<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminappbelitiket">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logobiru"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditArticleAct"></activity>
        <activity android:name=".EditWisataAct" />
        <activity android:name=".InputArticleAct" />
        <activity android:name=".AddArticleAct" />
        <activity android:name=".MyMenuAct" />
        <activity android:name=".InputAct" />
        <activity android:name=".HomeAct" />
        <activity android:name=".SignInAct" />
        <activity android:name=".GetStartedAct" />
        <activity android:name=".SplashAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>